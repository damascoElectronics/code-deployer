# .gitlab-ci.yml - Complete CI/CD pipeline with security analysis

# external files.
include:
  - local: '/ci/variables.yml'
  - local: '/ci/code-quality.yml'
  - local: '/ci/test.yml'
  - local: '/ci/coverage.yml'
  - local: '/ci/security.yml'
  - local: '/ci/quality_gate.yml'
  - local: '/ci/build.yml'
  - local: '/ci/report.yml'
  - local: '/ci/network.yml'

# stages
stages:
  - network
  - lint
  - test
  - coverage
  - security
  - quality-gate
  - build
  - deploy
